<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/header.ejs') %>
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <div id="show-page-intro">
        <h2> <%=listing.address%> </h2>
        <h3> <%=listing.city %></h3>
        <% const price = (listing.price) %>
        <h4><%= '$' + price.toFixed(0) %></h4>
    </div>
    <div class='container'>
    <% if (realtor.phone) var phoneNumber = realtor.phone.toString().split('') %>

    <p class='show-realtor'>MEET THE REALTOR</p>

    <div class='show-page'>
        <div class='realtor'>
            <img class='realtor-image' src='/uploads/<%= realtor.img %>' alt='realtor-id#-<%=realtor._id%>'/>
            <ul>
                <ul><%= realtor.name %></ul>
                <% if (phoneNumber) { %>
                <ul>
                    (<%= phoneNumber[0]%><%=phoneNumber[1]%><%=phoneNumber[2]%>)
                    <%= phoneNumber[3]%><%= phoneNumber[4]%><%= phoneNumber[5]%> - 
                    <%= phoneNumber[6]%><%= phoneNumber[7]%><%= phoneNumber[8]%><%= phoneNumber[9]%>
                </ul>
                <% } %>
                <ul><a href='/realtors/<%=realtor.id%>'>Realtor's Profile</a></ul>
            </ul>
            
        </div>
    
        <div class='show-house'>
            <img id='show-image' src='/uploads/<%= listing.img %>' alt='house-id#-<%=listing._id%>'>
            <div>
                <p>Property Details:</p>
                <ul>
                    <li>Size: <%=listing.size%> </li>
                    <li>Bedrooms: <%=listing.bedrooms%> </li>
                    <li>Bathrooms: <%=listing.bathrooms%> </li>
                </ul>
            </div>
        </div>

        <div class='white-space'>

        </div>
    </div>
    </div>
    <%- include('../partials/footer.ejs') %>
</body>
</html>